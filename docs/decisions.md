# Decision
- Use velvet voice by default.
- All the currently selected voice mp3 audio files should be preloaded when app starts or voice is changed.
- Voice MP3 files are preloaded into persistent reusable audio players for playback, but are not cached persistently by the service worker.